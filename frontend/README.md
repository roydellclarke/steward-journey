# StewardPath Frontend

Standalone Next.js frontend for StewardPath.

## Run Locally

```bash
cd frontend
npm install
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm run dev
```

The app runs on `http://localhost:3000` and calls the FastAPI backend.
