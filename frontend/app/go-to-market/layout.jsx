// Internal sales playbook. Never index; keep the acquisition strategy private.
export const metadata = { title: "Go to market", robots: { index: false, follow: false } };
export default function Layout({ children }) { return children; }
